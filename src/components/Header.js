import React, { Component } from 'react'

export default class Header extends Component {
    render() {
        return (
            <nav className="mid nav-bar">
                <h2 className="logo">Zen</h2>
            </nav>
        )
    }
}